machine:
  services:
    - docker
  environment:
    DB_CONNECTION_STRING: 'local.db'
dependencies:
  pre:
    - sudo chmod 777 ./ci/linux/init.sh && ./ci/linux/init.sh
    - ./ci/linux/setup.sh
test:
  override:
    - ./ci/linux/build.sh
    - ./ci/linux/test.sh
