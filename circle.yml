machine:
  services:
    - docker
dependencies:
  pre:
    - sudo chmod 777 ./ci/linux/setup.sh && ./ci/linux/setup.sh
test:
  override:
    - sudo chmod 777 ./ci/linux/build.sh && ./ci/linux/build.sh
