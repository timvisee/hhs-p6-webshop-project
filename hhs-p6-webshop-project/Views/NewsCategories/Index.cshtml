@model IEnumerable<hhs_p6_webshop_project.Models.NewsModels.NewsCategoryVM>

@{
    ViewData["Title"] = "Blog - Categorieen";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Maak nieuwe aan</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NewsCategory.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NewsCategory.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.NewsCategory.NewsCategoryID">Bewerk</a> |
                    <a asp-action="Details" asp-route-id="@item.NewsCategory.NewsCategoryID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.NewsCategory.NewsCategoryID">Verwijder</a>
                </td>
            </tr>
        }
    </tbody>
</table>
