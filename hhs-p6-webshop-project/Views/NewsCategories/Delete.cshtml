@model hhs_p6_webshop_project.Models.NewsModels.NewsCategoryView

@{
    ViewData["Title"] = "Blog - Verwijder " + @Model.NewsCategory.Name;
}

<h2>Verwijder @Model.NewsCategory.Name</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NewsCategory.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NewsCategory.Name)
        </dd>
    </dl>

    <form asp-action="Delete">
        <div class="form-actions no-color">
            <input type="submit" value="Verwijderen" class="btn btn-default" /> |
            <a asp-action="Index">Terug naar overzicht</a>
        </div>
    </form>
</div>
